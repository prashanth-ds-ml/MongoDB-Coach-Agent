<!-- source screenshot: Screenshot 2026-01-24 205538.png -->

27. What Python code is a valid aggregation that can be used with collection.aggregate(pipeline)?
(Choose 1)

A. { "$match": { "categories": "Bakery" } }

B. { "$match": { "categories" = "Bakery" } }

C. [ { "$match": { "categories": "Bakery" } }, { "$group": { "_id": "$stars" } } ]

D. [ { "$match": { "categories" = "Bakery" } }, { "$group": { "_id" = "$stars" } } ]
