version: "1"
project: my-awesome-tool

tasks:
  - name: process-data
    image: python:3.9-slim
    commands:
      - pip install -r requirements.txt
      - python scripts/process.py
    mounts:
      - ./data:/app/data
    env:
      - STAGE: production
