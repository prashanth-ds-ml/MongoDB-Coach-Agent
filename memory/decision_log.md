# Decision Log

Timestamped record of product and architecture decisions.

Related: [[Memory Home]], [[active_context|Active Context]], [[coach_flow_spec|Coach Flow Spec]], [[architecture_decisions|Architecture Decisions]]

## 2026-06-03T00:00:00+05:30
- Decision: Keep the coach anchored to a single syllabus topic and concept during Teach and Check modes.
- Reason: Prevents leakage into later topics such as CRUD querying or updates before the learner has earned that context.
- Decision: Allow only concept-scoped follow-up examples, with at most two examples and no forward topic previews.
- Reason: Keeps the explanation useful without confusing the learner with premature material.
- Decision: Document the active flow in markdown alongside code changes.
- Reason: The flow is part of the product behavior and needs to stay discoverable for future edits.
- Decision: Require a content contract version on every question-bank record and flag missing versions as legacy.
- Reason: Structural audit alone cannot distinguish legacy records from compliant records, and missing versioning is the simplest reliable legacy signal.
- Decision: Add a deterministic migration job that can promote, repair, or quarantine bank records before they reach practice.
- Reason: The practice loop must never depend on render-time cleanup to hide legacy data quality problems.

## 2026-06-11T00:00:00+05:30
- Decision: Treat exam preparation as the primary product goal and freeze immediately after the required study path is reliable.
- Reason: Optional platform work delays the learner from beginning exam preparation.
- Decision: Retire the fixed total question-bank target.
- Reason: Study readiness depends on concept coverage and valid practice workflows, not an arbitrary global count.
- Decision: Require each scheduled concept to have official documentation and five active validated questions.
- Reason: The daily learning loop promises an exact five-question gate and must not schedule concepts that cannot complete it.
- Decision: Execute the approved build order one phase at a time and review before continuing.
- Reason: This limits scope drift and prevents premature live-database operations.
- Decision: Use `qwen3.5:4b` as the default study model for the minimum 16 GB RAM / 6 GB NVIDIA GPU target.
- Reason: The interactive study path needs good-enough responses at practical local latency.
- Decision: Use `gemma4:12b` for question population and explanation repair.
- Reason: Offline content work can trade speed for stronger structured generation and repair quality.
- Decision: Treat legacy `MODEL` as a study-only compatibility setting.
- Reason: Population and repair must never silently inherit an interactive model chosen for speed.
- Decision: Use direct active-contract concept mapping as the single readiness rule for scheduling, practice, and completion.
- Reason: Broad topic fallback can falsely present unrelated questions and mark an untested concept complete.
- Decision: Refuse to start concept practice unless three active Easy and two active Medium questions are available.
- Reason: A total-only gate allows Hard or arbitrary questions to replace the intended first-pass learning progression.
- Superseded decision: Do not use `4 Easy + 3 Medium` or any other default population buffer.
- Reason: Only study readiness is fixed. Additional volume should respond to retake variety, weak areas, and mock needs.
- Decision: Keep `3 Easy + 2 Medium` as the readiness gate, but continue ordered population toward configurable per-concept inventory targets (`5 Easy + 5 Medium` by default).
- Reason: A readiness minimum is sufficient to unlock study, but a deeper inventory reduces repetition and improves practice and mock variety.
- Decision: Count only active-contract records toward study readiness.
- Reason: Legacy, repair-pending, and quarantined records cannot safely enter practice.
- Decision: Route explanation-only quality failures to `needs_explanation_repair` and structural/content failures to quarantine.
- Reason: Explanation repair can preserve sound questions, while structurally unsafe questions require manual review or replacement.
- Decision: Treat style diversity as guidance within requested generation slots, not as an additional quota.
- Reason: Style balancing must not create a hidden population target.
- Superseded decision: Default population fills only the `3 Easy + 2 Medium` readiness deficit; extras require an explicit Easy/Medium request.
- Reason: The readiness threshold should unlock study, but stopping there creates unnecessary repetition.
- Decision: Run Phase 4 repair and population in syllabus topic and concept order.
- Reason: Completing earlier concepts before later concepts creates a usable study path sooner and makes overnight progress predictable.

## 2026-06-16T00:00:00+05:30
- Decision: Implement model chain with quality gates for population and repair jobs.
- Reason: Single-model dependency risks quality failures; fallback to Cloudflare/OpenRouter APIs (already configured) provides resilience without additional local VRAM.
- Decision: Quality pipeline: Deterministic checks → Duplicate detection (stem hash) → LLM Judge (RAG-grounded against source markdown) → Retry with fix hint → Fallback model.
- Reason: Catches 40%+ issues free (format/casing), prevents duplicates early, verifies technical faithfulness to source docs.
- Decision: Structured JSONL logging per attempt to `logs/model_quality.jsonl` with verdict, flags, latency, tokens, model, source files.
- Reason: Enables post-run analysis of fallback rates, duplicate rates, model performance, prompt tuning.
- Decision: Circuit breaker per model (3 failures → 5 min cooldown) prevents repeated calls to degraded endpoints.
- Reason: Protects overnight run throughput from stalled models.
- Decision: Require `source_files` metadata on every generated question for RAG judge verification.
- Reason: Judge needs ground truth to verify answer/explanation faithfulness without human expertise.
- Decision: Use external reference repo (yixin0829/mongodb-dev-cert-prep) as exam-fidelity benchmark.
- Reason: Provides 22 verified CRUD sub-objectives, PyMongo syntax patterns, and "Given scenario → identify correct output" question format matching certification style.
- Decision: Do not pull additional local models; use existing `gemma4:12b` + API fallbacks.
- Reason: VRAM unload/load cycles (30-60s) destroy throughput; API fallbacks are fast and free-tier sufficient.
- Decision: Use direct HTTP adapters for OpenRouter and Cloudflare Workers AI in `model_runner.py` instead of optional LangChain provider packages.
- Reason: The repair and population jobs should not fail when `langchain_openrouter` or `langchain_cloudflare` are absent; the provider APIs already expose stable HTTP endpoints, and the model runner only needs text completion responses.

## 2026-06-16T00:00:00+05:30 (Implementation Complete)

- Decision: Implement `model_runner.py` with full quality gate pipeline.
- Reason: Centralize multi-provider calling, deterministic checks, duplicate detection, circuit breaker, and JSONL logging in one module.
- Decision: Implement `judge_questions.py` with RAG-grounded verification.
- Reason: Verify technical faithfulness to source docs, catch invented types (Topic 1), validate explanation structure, and check context grounding.
- Decision: Wire quality gates into `nightly_seed_questions.py` and `repair_explanations.py` via `model_runner.generate_with_quality_gate()`.
- Reason: Replace fragile direct LLM calls with robust quality-gated generation that logs every attempt.
- Decision: Add `source_files` metadata field to `database.save_generated_question()`.
- Reason: Judge needs ground truth source files to verify answer/explanation faithfulness without human expertise.
- Historical decision (superseded): Configure cloud-first model chains: OpenRouter `gpt-oss-120b`/`gpt-oss-20b` → Cloudflare `@cf/meta/llama-3.3-70b-instruct` → Local Ollama fallback.
- Reason: Local models (`gemma4:12b`, `qwen3:14b`, `qwen2.5-coder:7b`) were timing out on structured JSON output; cloud models provide reliable structured output with free-tier rate limits.
- Status: Superseded by the later local-first ordering decision once the prompt/response contract was tightened and the string-vs-dict validation split was implemented.
- Decision: Use direct HTTP adapters for OpenRouter and Cloudflare in `model_runner.py` instead of optional LangChain provider packages.
- Reason: Eliminates optional dependency failures; provider HTTP APIs are stable and the runner only needs text completion responses.
- Decision: Prioritize local Ollama models before cloud fallback providers for population and repair chains.
- Reason: The tightened prompt/response contract should try the local model first to minimize remote latency and API dependency, while keeping cloud-free APIs as fallback if Ollama fails.
- Decision: Treat population and repair as separate response contracts.
- Reason: Population emits string options plus a `correct_answer`, while repair emits the seven-part explanation schema; the quality gate must validate them differently to avoid false failures.

## 2026-06-17T00:00:00+05:30

- Decision: Population now uses a lean `question_shell` contract and hands successful shells to immediate explanation repair.
- Reason: The full seven-part explanation contract was too expensive and unreliable on the population path, especially under local-first throughput constraints.
- Decision: Keep the seven-part explanation schema in the repair job, not the population job.
- Reason: Repair is the right place to produce rich explanation text; population should only create the minimum viable MCQ shell.

- Date: 2026-06-18
- Decision: `apply_repair()` must write `content_contract_*` fields under `metadata.*`.
- Reason: Top-level contract metadata leaves repaired shells stuck in `needs_explanation_repair`, which hides active records from readiness counts.

- Date: 2026-06-18
- Decision: `question_bank_comparison_report` must count stored `needs_explanation_repair` records directly, and `repair_explanations` must select repair candidates from that stored backlog.
- Reason: Audit-only reclassification undercounts repairable Topic 1 backlog and lets the sequencer skip ahead to later topics before Topic 1 is actually cleared.

- Date: 2026-06-18
- Decision: Split legacy classification into `repair` for explanation-only fixes and `regenerate` for structurally bad or Topic 1-rescued records, with a new `needs_question_regeneration` status.
- Reason: Topic 1 backlog contains both explanation-only repairs and question-shape problems; treating them as one category hides salvageable records and makes the sequencer inaccurate.

- Date: 2026-06-18
- Decision: Count repair backlog by exact `topic_id + concept` scope instead of free-text topic labels.
- Reason: Legacy Topic 1 records can carry malformed `metadata.topic` values even when the topic id and concept are correct, so free-text matching hides backlog from the selector.

- Date: 2026-06-18
- Decision: Include non-target difficulty records in concept-level backlog counts.
- Reason: Hard-difficulty records can still need repair and should keep a concept from appearing clean before the backlog is actually cleared.

- Date: 2026-06-18
- Decision: Add a repeat-until-clean overnight runner mode that reselects the next incomplete concept within the same topic after each repair/population pass.
- Reason: Topic 1 backlog is concept-ordered and can be drained more reliably by looping the existing repair/populate scripts until the selected concept is clean or the max-cycle cap is reached.

## 2026-06-18T00:00:00+05:30

- Decision: Run `mark_scope_leaks` before and after repair/population and quarantine future-scope records instead of leaving them active.
- Reason: Scope leaks are learner-facing regressions; quarantining them keeps practice strictly within the current syllabus concept.
- Decision: Advance Topic 2 in canonical order after `insertOne()` and `insertMany()` are clean, then move to `_id and ObjectId`.
- Reason: The ordered repair/populate loop is working, and moving forward only after the current concept is clean keeps the bank scoped and predictable.

- Date: 2026-06-18
- Decision: Allow bank-wide syllabus remapping to retag concept buckets, but treat any selector regression it triggers as a canonical-order checkpoint.
- Reason: Remapping fixes stale labels and mis-tagged questions, but it can also re-expose earlier-topic work; the selector must remain the source of truth before advancing.

- Date: 2026-06-18
- Decision: Add concept-specific variation guidance to Topic 2 population prompts, especially for `insertMany()`, to avoid repeated return-type questions.
- Reason: The duplicate gate was rejecting many near-identical `insertMany()` questions; explicit variation guidance improves throughput without weakening the quality checks.

- Date: 2026-06-18
- Decision: Add a Topic 2 `_id and ObjectId` stem guard that rejects questions which do not explicitly mention `_id` or `ObjectId`, and quarantine malformed active records instead of leaving them learner-facing.
- Reason: The quality gate allowed a malformed `_id and ObjectId` stem through even though it was not concept-scoped enough for safe practice; the new guard keeps the bank aligned with the syllabus and removes ambiguous items from the active set.

- Date: 2026-06-18
- Decision: Quarantine the final generic CRUD record that was left in Topic 2 `_id and ObjectId` after repair retries failed, and advance the selector to Topic 3 `find()`.
- Reason: The remaining record was not actually scoped to `_id and ObjectId`, so quarantining it prevented a future-scope leak and completed Topic 2 in canonical order.

- Date: 2026-06-18
- Decision: Add `-SingleQuestion` mode to `scripts/run_phase4_overnight.ps1` so repair and population can be forced to one record at a time while still using the repeat-until-clean loop.
- Reason: Topic 3 `find()` and similar concepts can stall on batch-level quality gates; single-question passes reduce timeout risk and make it easier to inspect and fix the exact failing record in the morning.

- Date: 2026-06-18
- Decision: Add a mandatory Topic 3 `find()/find_one()` repair checklist and let the overnight runner log repair/population batch failures without aborting repeat-until-clean mode.
- Reason: `findOne()` repairs were repeatedly missing required explanation fields, so the prompt now requires explicit cursor-vs-single-document wording and the runner keeps going long enough to capture the failure in logs rather than stopping the whole overnight pass.

- Date: 2026-06-19
- Decision: Force overnight long-run Topic loops to local-only model chains and close Topic 3 once the selector advances to Topic 4.
- Reason: OpenRouter and Cloudflare fallback calls were wasting time with repeated 402/400 failures, while Ollama was already succeeding; once Topic 3 reached full selector readiness, the next canonical target became Topic 4 `replaceOne()`.

- Date: 2026-06-19
- Decision: Hard-delete only quarantined records that are clearly unrecoverable or off-domain, and leave MongoDB-aligned quarantined concepts for remap or repair.
- Reason: Most quarantined records still map to syllabus concepts and should be preserved for remediation; only blank or clearly non-Mongo records were safe to remove without risking syllabus coverage.

- Date: 2026-06-19
- Decision: Process repair-pending and quarantined records by exact `topic_id + concept` in canonical syllabus order, using the same repair/populate loop.
- Reason: Flat status-based cleanup hides topic-specific backlog and makes it easy to miss mis-tagged or future-scope records; concept-scoped looping keeps remediation aligned with syllabus order and the selector.

- Date: 2026-06-20
- Decision: Treat the question-bank maintenance process as a durable cross-session loop: `selector -> exact topic/concept -> one question at a time -> repair/quarantine/validate -> recheck selector -> repeat` until every concept reaches the active inventory target.
- Reason: The bank is only ready when both the readiness gate and the population target are satisfied, and the workflow must survive restarts without losing the canonical topic/concept order.
- Decision: Add a hard leak guard for Topic 4 `replaceOne()` generation so future CRUD concepts and update operators are rejected before insertion.
- Reason: `update_one`, `update_many`, `$set`, and related later-scope terms were causing new `replaceOne()` questions to be quarantined instead of becoming active.
- Decision: Standardize the session status line to report active topic/concept, repair pending, quarantined total, quarantined repairable, hard-delete candidates, and population deficit before every one-question pass.
- Reason: The loop only works cleanly when each session starts with the same inventory snapshot and the same decision inputs.
- Decision: Scope every loop report and decision strictly to the current topic and current concept unless a broader bank-wide summary is explicitly requested.
- Reason: Cross-topic counts are useful for global reviews, but the live maintenance loop must stay focused on the active concept to avoid skipping backlog work.
- Decision: Require Ollama JSON mode in both local model adapter paths.
- Reason: Prompt-only JSON instructions still produced repeated parse failures from `gemma4:12b`; provider-level JSON mode improves response reliability before deterministic quality checks.
- Decision: Repair valid quarantined `replaceOne()` records before generating more shells when the quarantine bucket already contains usable MongoDB-aligned material.
- Reason: The generator repeatedly produced near-duplicate questions with prohibited future-scope distractors, while existing quarantined records can be corrected, revalidated, and promoted without increasing bank noise.
- Decision: Classify every quarantined record against the canonical syllabus before repair, but never activate a record as part of classification.
- Reason: Stem/correct-answer evidence can recover badly mapped questions, while separate validation, duplicate, and scope checks prevent an apparently confident mapping from becoming learner-facing prematurely.
- Decision: Preserve two inactive quarantine dispositions: `needs_manual_classification` for overlapping concepts and `keep_aside_misc` for contentless, off-domain, or unsupported questions.
- Reason: Forcing every record into a syllabus bucket would recreate the mapping errors this cleanup is intended to remove.

- Decision: Treat `quarantine_pending` as an incomplete concept in `next_phase4_topic` and triage quarantined records for the selected topic/concept before repair/population in the overnight runner.
- Reason: Quarantined backlog can be skipped if the selector only watches repair/regeneration/legacy statuses, so the maintenance loop must keep quarantined and repair-pending work in the same concept-scoped pass.

## 2026-06-29T00:00:00+05:30

- Decision: Start lesson prebuild in canonical syllabus order from Topic 1 `BSON Data Types`, separate from the active Phase 4 question-bank selector.
- Reason: Stored lessons should follow the syllabus contract from the beginning, while question-bank readiness must continue on its own ordered backlog.
- Decision: Store prebuilt lessons as concept-scoped artifacts in MongoDB with statuses `validated` or `needs_review`, and let the CLI prefer validated stored lessons before live generation.
- Reason: This removes learner-facing latency only when the lesson is already contract-compliant and avoids exposing unstable drafts.
- Decision: Run lesson prebuild with one corrective retry, but keep the better draft if the repair attempt degrades structure.
- Reason: The local lesson model can partially follow the contract, but naive repair retries can collapse the output further; the pipeline must preserve the stronger attempt.

## 2026-06-30T00:00:00+05:30

- Decision: Register a canonical lesson-completion loop per concept: `source bundle -> full lesson draft -> validation -> targeted repair -> missing-section generation -> validation -> store validated lesson -> concept-local practice audit -> remap/quarantine misaligned questions -> readiness recheck`.
- Reason: A lesson is not complete when the text alone is valid; the learner-facing concept loop is only trustworthy when the stored lesson and the active `3 Easy + 2 Medium` question set are aligned to the same concept boundary.
- Decision: Keep the lesson/practice cleanup local to the current concept before moving to the next concept.
- Reason: Topic-level or bank-wide cleanup during lesson build would blur scope, slow progress, and make it harder to prove that one concept is truly ready end-to-end.
- Decision: For Topic 1 `BSON Data Types`, treat `Collections vs Tables`, `Document structure`, document-relationship stems, query-operator leaks, and obvious junk stems as out of scope for the BSON lesson audit.
- Reason: The validated BSON lesson teaches BSON types, numeric/date precision, arrays, embedded documents, and `ObjectId`; questions outside that boundary create learner confusion even if they are still broadly MongoDB-related.
- Decision: Treat Topic 1 scope leakage as a validation failure, not a prompt preference.
- Reason: Prompt rules alone allowed `Document structure` and `Collections vs Tables` lessons to pass while still leaking `find()`, `findOne()`, `insertOne()`, projection, dot notation, and query terminology. Topic 1 lessons must fail until those future-topic references are removed.
- Decision: Add concept-specific cleanup for Topic 1 lessons after generation, including section regeneration for leaky sections and final concept-specific scrubs for `Document structure` and `Collections vs Tables`.
- Reason: The local lesson model can still sneak a few future-topic lines through even after prompt tightening; the concept loop needs deterministic cleanup to keep beginner-facing lessons strictly bounded.
- Decision: Add a hard `updateOne()` question-scope guard and variation brief that reject full-document replacement semantics and `replace_one()/replaceOne()` as the correct answer.
- Reason: The Topic 4 `updateOne()` pool had drifted completely into `replaceOne()` content, so the validator and prompt needed explicit concept-boundary enforcement before refill.
- Decision: Add the same hard scope guard and variation brief for `updateMany()` so multi-document update questions stay on update-operator semantics and do not drift into replacement wording.
- Reason: `updateMany()` is adjacent to `updateOne()` and shares the same replacement-vs-update failure mode; the generator needs concept-specific rejection logic before it can reliably populate the bank.
- Decision: Relax the syntax-example requirement for Topic 4 operator-family concepts and keep the operator-specific prompts focused on the update behavior itself.
- Reason: The repair model repeatedly failed `Topic 4` operator questions on a missing syntax-example section even after producing a correct explanation, so the hard gate blocked progress on otherwise valid content.

## 2026-07-03T00:00:00+05:30
- Decision: Use the OpenRouter Free Models Router (`openrouter/free`) for programmatic remote LLM execution.
  - Reason: Resolves credit/billing and 402/404 errors associated with paid model names when no payment is registered, while maintaining high-quality outputs.
- Decision: Integrate active exam-bank question stems directly into the lesson enhancement prompts.
  - Reason: Rather than generating general lessons, this forces the LLM to cover the exact technical rules, bit-lengths, and behaviors tested by the actual exam questions, ensuring every lesson is top-level prep material.
- Decision: Implement automated post-processing (`clean_topic_1_leaks`) in the lesson enhancer script to clean vocabulary scope leaks before validation.
  - Reason: The LLM occasionally uses forbidden words like "query", "queries", or "projection" inside descriptions of traversability and BSON layout. Programmatically cleaning these allows the lesson to pass strict validation without losing conceptual depth.
- Decision: Add skip-if-already-enhanced check in `scripts/enhance_all_lessons.py` before calling the LLM.
  - Reason: The bulk enhancer job runs for many minutes and can be interrupted by rate limits. Checking for the presence of the local markdown export file at the start of each concept allows clean, zero-duplication resumption without manually tracking completed concepts.
- Decision: Store NVIDIA API key as `NVIDIA_API_KEY` in `.env`; accept legacy bare `nvidia` var for backward compat in `model_runner.py` and `enhance_all_lessons.py`.
  - Reason: The initial `.env` stored the key without a standard env-var name (`nvidia = nvapi-...`), which caused the NVIDIA route to never activate. Canonical naming makes the config discoverable and consistent with industry convention.
- Decision: Add a 10-second inter-request sleep delay in `scripts/enhance_all_lessons.py`.
  - Reason: OpenRouter free-tier endpoints enforce per-minute rate limits. A 10-second gap between lessons (approx. 6 requests/min) keeps the batch below the throttle threshold without making individual lessons noticeably slower.
- Decision: Export enhanced lessons as local markdown files to `memory/lessons/` in addition to MongoDB storage.
  - Reason: Local exports give agents, Obsidian, and offline review tools direct access to lesson content without a live MongoDB connection. The files serve as a convenience cache; MongoDB `lesson_artifacts` remains the source of truth.

## 2026-07-04T00:00:00+05:30 (approximate; backfilled 2026-07-05 -- these decisions were made and implemented in code but never logged at the time)

- Decision: Add a `provenance` sub-document (`draft -> sourced -> confirmed/suspect`) to every question, independent of the existing content-contract lifecycle, and require both `is_contract_active()` and `is_confirmed()` (`provenance.state == "confirmed"`) before a question is practice-ready.
  - Reason: The content-contract check only verifies structural well-formedness, not factual correctness; that conflation is what let factually wrong content ship as "active" throughout the bank. Provenance tracks the orthogonal question of whether a human has verified a specific item against its cited source.
- Decision: Verify citations deterministically (exact, whitespace-normalized substring match against the named source file) and never use an LLM to judge factual truth.
- Reason: An LLM "fact-checking judge" is itself unverifiable; a deterministic string match is the only check that can be trusted without a human in the loop.
- Decision: Add a self-consistency check as a second, separate gate after the citation check, explicitly scoped to internal coherence only (does the explanation support the marked answer, are options distinct) -- never described or used as a MongoDB fact-checker.
  - Reason: Internal coherence and external factual correctness are different failure modes and must not be conflated into one model call or one verdict.
- Decision: The only path to `confirmed` is a one-at-a-time human review screen (`certcoach-review-questions`); no batch-table review UI.
  - Reason: Reviewing a table of N questions at once turns confirmation into rubber-stamping, defeating the purpose of the review being the actual fact-check.
- Decision: Backfill every existing question to `provenance.state = "draft"`, then move backfilled `draft` questions with an empty citation quote straight to `suspect` (`backfill_provenance --suspect-uncited`), rather than leaving them in the review queue.
  - Reason: A backfilled record with no verbatim quote can never pass the deterministic citation check as-is (legacy generation only ever stored a filename/title/URL hint, never a quote); leaving guaranteed-fails in the active review queue would waste review effort with no possible good outcome.
- Decision: `analyze_backlog.py` is read-only and writes nothing; it only classifies the `suspect` backlog (duplicate / has-doc-lead / no-doc-lead) so a human decides, per class, whether to delete or keep as a regeneration target.
  - Reason: No repair-in-place path exists for a missing citation quote -- it cannot be fabricated after the fact -- so this backlog needs a human decision, not an automated repair attempt.
- Decision: Treat `pics_qa/` screenshot-sourced questions as a distinct recovery track (`reocr_pics_qa` -> `recover_screenshot_citations` -> `purge_screenshot_backlog`) separate from the general legacy backlog.
  - Reason: The original image-extraction pipeline discarded the raw OCR transcript and kept only the structured MCQ, so no citation was ever possible for these until the screenshots were re-OCR'd; a live test also showed the original extraction sometimes rewrote question content away from the actual screenshot, meaning some of these records are not safe to keep even as regeneration targets.
- Decision: Add multi-response ("select all that apply") question support (`response_type: single|multi`) end-to-end -- generation schema, validation, storage, and CLI input/scoring.
  - Reason: The real exam includes multi-select items; single-answer-only modeling could not represent them.
- Decision: Select mock-exam questions by real exam domain weights (`EXAM_DOMAIN_WEIGHTS`) from `confirmed` inventory only, with a per-concept round-robin cap inside each domain's share and an explicit shortfall report instead of silent padding.
  - Reason: An even split across topics misrepresents the real exam's weighting (CRUD Operations alone is 51%); silently padding a shortfall would hide the fact that a domain lacks enough confirmed inventory.
- Decision: Wrong-attempt remediation is a stateless lookup only (the missed question's own citation, plus a small random sample of domain-matched flashcards) -- no new explanation generation, nothing saved, and no per-item spaced-repetition scheduling engine.
  - Reason: There is no scheduling engine to hook flashcards into, and building one was out of scope; surfacing existing material honestly is sufficient and does not overstate what the system does.

## 2026-07-05T00:00:00+05:30

- Decision: Fix `analyze_backlog.py`'s `_has_real_doc_lead()` to also check topic-level doc coverage (`data/cleaned_markdowns/topic_{NN}_*`), not just an exact `citation_source`-as-filename match.
  - Reason: Every legacy question's `citation_source` is a human-readable title or a mongodb.com URL, never the corpus's actual filenames, so the filename-only check reported 0 `has_doc_lead` across the entire 376-question suspect backlog even though all 12 syllabus topics have real official docs on file. Left as-is, the report would have wrongly signaled that ~350 legitimately-regenerable legacy questions were safe to delete.
- Decision: Before committing to any interpretation of an undocumented backlog script's output (e.g. "safe to delete"), verify the classification logic against a sample of real records rather than trusting the docstring's stated intent.
  - Reason: `analyze_backlog.py`'s docstring described the intended behavior correctly, but the implementation did not match it; only checking actual `citation_source` values on real Topic 3 questions surfaced the mismatch.

## 2026-07-06T00:00:00+05:30

- Decision: Drop the planned `inspect_doc.py` (a per-doc "how many questions can this doc yield" estimator) before building it out further.
  - Reason: The user redirected toward the simplest possible working loop -- read doc, generate MCQ, verify, review, practice -- and a yield-estimation step wasn't part of that loop; the underlying doc-pull/generation pipeline already worked correctly with a full doc as context, so estimating first would have added scaffolding without fixing anything real.
- Decision: `nightly_seed_questions._get_db_style_counts()` must gate on `database.is_practice_ready()` (contract-active AND confirmed), not `is_contract_active()` alone, when computing population deficits.
  - Reason: After the provenance backfill, almost the entire legacy bank is contract-active but `suspect`; counting contract-active alone made the deficit calculator think every concept already had enough inventory, so it would never generate anything. Confirmed via a live dry-run that went from "0 slots needed" to the correct "10 slots needed" for Topic 1 BSON Data Types after the fix.
- Decision: `database.verify_citation()` must normalize markdown emphasis punctuation (`` ` ``, `*`, `_`) on both the quote and the doc text before the verbatim substring check, not just whitespace.
  - Reason: A live generation run produced a citation quote that was genuinely verbatim except the source doc wraps the same terms in markdown backticks (e.g. `` `$type` ``); the whitespace-only check rejected it as fabricated. Confirmed the model was not hallucinating -- it correctly located the exact sentence in a 24K-character doc; the checker's normalization was too strict.
- Decision: Switch the self-consistency check's default model from `deepseek-r1:8b` to `qwen2.5-coder:7b`.
  - Reason: Benchmarked 5 local models on a real consistent question and a deliberately-broken one (correct-answer flag swapped without touching the explanation). `deepseek-r1:8b` produced 14,000+ characters of reasoning and still timed out before reaching a verdict. None of the 5 models reliably caught the deliberately-broken case (all read the explanation prose in isolation rather than cross-checking it against the metadata), so model choice was decided on speed and correctness on the genuinely-good case, not on the unsolved cross-check weakness, which is a separate, deferred problem.
- Decision: Simplify the review screen's Citation panel to just source file, cited quote, and deterministic-check result; remove the "Source excerpt" preview entirely rather than fix it.
  - Reason: The excerpt's own quote-matching logic didn't share the markdown-normalization fix above, so it was falling back to showing the top of the file instead of real context. Once the source filename became a clickable local-file link (this session), the excerpt was redundant anyway -- one click gives full context on demand.
- Decision: `planner.score_md_file_for_concept()` must strip `$` from concept tokens before matching against filenames.
  - Reason: Building the full syllabus-to-docs study order map surfaced that every bare-operator concept (`$set`, `$elemMatch`, `$match`, `$group`, `$project`, `$lookup`, `$out`, etc.) scored 0 against its own dedicated reference doc, because official doc filenames never contain a literal `$`. This silently degraded doc selection for Topics 4, 7, and 8 in both lesson generation and question population, not just for the study-order map.
- Decision: Delete `scratch/` (24 one-off debug scripts) entirely rather than triage file-by-file.
  - Reason: Already gitignored and untracked (nothing lost from history), unreferenced by any runtime or test code, and it was the source of the known pytest-collection hazard (`test_zhipu_vision.py`). `src/antigravity_cli/` and `src/scripts/` were evaluated and kept: the former is a separate, previously-flagged-as-undecided tool (not this session's call to make), the latter still has real test coverage and may be needed again for future doc ingestion.
- Decision: `map_questions_to_docs.py` is read-only and writes nothing to the database, even though the topic/concept it infers for topic-id-less records could technically be persisted the same way `certcoach-map-questions` does.
  - Reason: Given a direct choice, the user asked for the report only, no DB writes -- inferred placements are a best-effort keyword match, not a verified fact, and persisting them silently would blur the line between "inferred for a report" and "confirmed correct."
  - Reason (secondary): keeps this diagnostic script auditable on the same terms as `analyze_backlog.py` -- a human decides what to do with the findings, the tool doesn't act on them.

## 2026-07-06T00:00:00+05:30 (continued)

- Decision: Wipe `user_attempts`, `user_study_sessions`, `user_profiles`, and `lesson_artifacts` (20/4/3/58 documents respectively) to give the user a genuinely fresh learner-history state, backed up first to `backups/learner-history-backup-20260706-201216/`.
  - Reason: User explicitly asked to start over; the question bank (`certcoach_db.questions`) and login (`users`) were deliberately excluded since they're content/identity, not history.
- Decision: Also delete the 2 `confirmed` BSON Data Types questions (Topic 1, concept #1 in canonical study order) and reset that concept to zero, backed up to `backups/bson-data-types-confirmed-backup-20260706-210908/`.
  - Reason: User explicitly chose "discard and redo" over keeping the already-confirmed content, wanting concept #1 to start from true zero rather than carry over prior work.
- Decision: Purge the 25 screenshot-sourced suspect questions (`certcoach-purge-screenshot-backlog`) rather than hold them.
  - Reason: Already confirmed unrecoverable in a prior session (no supporting quote findable in the OCR transcript) -- they can never become `confirmed` as-is, so holding them served no purpose.
- Decision: Fold the 23 orphan (`topic_id: None`) suspects into the per-concept seeding loop as regeneration signal rather than purge or hold outright.
  - Reason: `map_questions_to_docs.py` already gives all 23 an inferred topic/concept and doc lead; deleting them would throw away that signal, and they can't become `confirmed` directly anyway (same rule as all suspects), so folding them in costs nothing.
- Decision: Split the 3-sessions-deep uncommitted provenance-system work into two commits (provenance/trust rollout; flashcards.json data sync) instead of one bundled commit, and delete the untracked `sc_raw_output.txt` (garbled debug text) rather than commit it.
  - Reason: The flashcards.json changes across `data/`, `mobile/assets/`, `web-flashcards/` are an unrelated content sync, not part of the provenance work -- bundling them would muddy the history. The stray file had no recognizable content and no reference anywhere in the codebase.
- Decision: Make population targets scale with each concept's real MongoDB exam-blueprint weight instead of a flat `5 Easy + 5 Medium` (or whatever `--target-easy`/`--target-medium` default) applied identically to all 58 concepts.
  - Reason: User pointed out the flat default ignores that concepts vary enormously in real exam weight (Tools & Tooling is 2% of the exam; CRUD is 51%) and that the doc corpus can't necessarily sustain a fixed count everywhere anyway. `question_targets.build_weighted_targets` now cascades `EXAM_DOMAIN_WEIGHTS` (the same real domain percentages the mock exam already used, previously never touched by population) down through each topic and concept; `nightly_seed_questions.audit_weighted_deficits` now actually consumes `target.target_count` from that cascade for the default (no-explicit-override) path, which it previously computed and then discarded entirely.
  - Reason (floor stays fixed): the `3 Easy + 2 Medium` readiness gate is *not* touched by this change -- it mirrors a hardcoded five-question practice-session composition in `cli.py` (`unique_easy[:3] + unique_medium[:2]`), a live product/UX decision, not a population-planning artifact. Lowering it would not unlock more practice sessions without a separate, unrequested change to `cli.py`.
  - Reason (CRUD's internal split): the official exam guide gives no sub-breakdown for CRUD's 51% across its 7 topics (Create/Read/Update/Delete/Query Operators/Arrays/Aggregation). Per the user's explicit choice, each topic's existing High/Medium/Low label (already in `syllabus.json`) is used as a relative multiplier (High=1.0, Medium=0.6), normalized within the domain -- the closest exam-relevant signal already present in the data, rather than an equal split or a new invented number.
  - Reason (Hard excluded): `difficulty_distribution()` computes an Easy/Medium/Hard split, but active population/practice generation has never included Hard-difficulty question generation; adding it would be a separate, unreviewed scope expansion, so only the Easy:Medium ratio is consumed (renormalized without Hard) via the new `easy_medium_split()`.
  - Reason (adaptive, not mandatory): a concept that can't reach its computed target after `max_attempts_per_slot` retries is now recorded in a shortfall report at the end of `certcoach-seed-nightly`, mirroring the weighted mock's existing "explicit shortfall, never silent padding" pattern, instead of treating the target as a hard requirement the doc corpus must satisfy.

## 2026-07-07T00:00:00+05:30 (session 6)

- Decision: Audited the user's full 12-step study-loop journey against the repo; rebuilt the two real gaps (combined doc-study + dry-run yield, and a React review client) and deferred a third (adaptive coach + spaced revision) rather than build all three.
  - Reason: Steps 10-11 (adaptive coach steering weak-area practice, bullet-point mistake notes + spaced revision) were already deliberately cut in an earlier session to a stateless remediation lookup (see `get_remediation_for_wrong_attempt` in `database.py`, "spec point 10" comment) and documented as "no spaced-repetition scheduling engine exists or is planned." Reopening that now would violate `AGENTS.md`'s own rule against optional product features before preparation-readiness blockers clear. User confirmed: revisit only after 3 topics/concepts are mastered (see `[[project_certcoach_memory_staleness]]`-adjacent memory `project_adaptive_coach_deferred` in the personal memory store, not this vault).
- Decision: Rebuilt `inspect_doc.py` (deleted, never committed, previously worked on a bare `--doc filename`) as `certcoach-preview-concept --topic --concept`, scoped by syllabus topic/concept instead of a bare filename, combining "load doc as study material" and "dry-run its question yield" into one command rather than two.
  - Reason: The bare-filename version was decoupled from the concept-scoped resolution every other part of the pipeline uses (`planner.load_md_context`/`prioritize_md_files`), which is likely why it felt like extra scaffolding and got dropped. The rebuild reuses that same resolution, plus the real pipeline's own citation gate (`database.verify_citation`) to verify each candidate fact's quote -- so the yield estimate reflects exactly the standard a generated question must clear later, not a separate looser heuristic. Candidate-fact extraction reuses `model_runner.get_model_runner()._call_model(...)`, the same low-level model-call primitive `lesson_aligned_practice_builder.py` already uses for non-MCQ auxiliary prompts (not `nightly_seed_questions._ollama_json_request`, which is reserved for Ollama keep-alive/preload pings, not content generation).
- Decision: Converted `certcoach-review-web` ("Docket")'s frontend from inline vanilla-JS/HTML (`_INDEX_HTML` in `review_api.py`) to a standalone React+Vite app (`review-web/`), keeping the FastAPI backend's routes and `_serialize_question` contract unchanged.
  - Reason: The user's stated journey explicitly wants a "react based tool," and the repo already has React+Vite tooling conventions to match (`web-flashcards/`) -- building the review client in plain JS was a mismatch with the user's own stated requirement, not an oversight to route around. `review_api.py`'s `GET /` now returns a small JSON info body instead of serving HTML, and `CORSMiddleware` was added (`DOCKET_CORS_ORIGINS` env override, defaulting to the Vite dev origin) so the React app can call it cross-origin in dev. The two-column citation-side-by-side layout (`QuestionCard.jsx` + `CitationPanel.jsx`) reproduces the 2026-07-07-session-5 amber "Confirm is unavailable" banner fix verbatim rather than re-deriving it, since that was a real bug fix, not incidental styling.
- Decision: Saved the learner-journey artifact into the repo at `memory/diagrams/learner_journey.html` (linked from `Memory Home.md`) instead of leaving it only in the session's temporary scratchpad.
  - Reason: The scratchpad directory is session-scoped and gets cleaned up; the diagram is a durable reference for future sessions checking build status against the intended journey, matching how other durable references live under `memory/`.
- Decision: Removed `run_ai_question_wizard()` (`cli.py`, reachable from `Study Settings & Extras -> f`) and its `validate_lexical_syntax_guard` wrapper; replaced the menu entry with `run_question_bank_reports()`, keeping only the two legitimate read-only reports (Quality Analytics, Seven-Part Explanation Coverage Audit) that used to share the same function.
  - Reason: Auditing the live CLI against the just-built journey surfaced that this wizard was broken, not just superseded -- `quiz_generator.generate_quiz_for_topic()` depends on a `chroma_db` vector store that doesn't exist anywhere in this repo, so every generation attempt threw, was caught, and silently fell back to the exact same hardcoded mock question ("insertOne into the catalog collection...") regardless of which topic the user picked. It also never captured a real citation quote, so anything it wrote could never pass `database.verify_citation` or reach `confirmed` -- not a practice-safety hole, but dead-end scaffolding that wrote through a separate `draft_questions_col`/`approve_draft_question` mechanism unrelated to the real provenance pipeline (`certcoach-preview-concept` / `certcoach-seed-nightly` / `certcoach-review-web`), while its "Approve -> Add to Production Bank" language misleadingly implied otherwise. User chose removal over leaving it as a known issue.
  - Note: `database.save_draft_question`, `approve_draft_question`, `get_draft_questions`, `draft_questions_col`, and `core/quiz_generator.py` are now fully orphaned (no remaining callers anywhere in `src/certcoach`) -- left in place since removing them is a separate, larger cleanup the user hasn't asked for yet, not because they're still needed.
- Decision: Removed the now-orphaned draft-question machinery entirely -- `database.save_draft_question`, `get_draft_questions`, `approve_draft_question`, the `draft_questions_col` global (both its initial assignment and its reassignment inside `update_database_connection`), and deleted `src/certcoach/core/quiz_generator.py` + `src/certcoach/core/retriever.py` (`CertCoachRetriever`) outright.
  - Reason: User confirmed removal after the prior session flagged these as fully orphaned (no callers anywhere in `src/certcoach` once `run_ai_question_wizard()` was gone). `quiz_generator.py`'s own top-level import (`from retriever import CertCoachRetriever`, a bare, non-package-qualified import) was likely already broken independent of the missing `chroma_db` directory -- either way, nothing else in the codebase ever called it, so silent failure would never have surfaced anywhere except through the now-removed wizard. Confirmed with a repo-wide grep before deleting: only a separate, previously-reviewed legacy copy under `src/scripts/core/` (kept per the 2026-07-06 `src/scripts/` retention decision) still references these names -- that tree is out of scope here and untouched. 280/280 tests pass unchanged (no test ever referenced these symbols).

## 2026-07-07T00:00:00+05:30 (session 7)

- Decision: Decoupled `run_teach_session`'s "Learn" agenda item from question-bank readiness -- `planner.get_syllabus_status()` now picks `next_topic` based on doc coverage + an uncompleted concept (`readiness_concepts`), not `ready_subtopics` (the 3E+2M practice-readiness filter).
  - Reason: Live symptom -- the user accepted the 12-day study plan and "Start Today's Study Agenda" reported all 58 concepts blocked, including concept #1, because `generate_daily_agenda()` only ever emitted a `"Learn"` item for a topic whose active concept had already cleared the 3E+2M floor. The lesson/Q&A pipeline (`lesson_bank.py`, `certcoach-prebuild-lesson`, `certcoach-preview-concept`) has zero dependency on question count -- the coupling was pure orchestration, not an architectural requirement -- so a concept with zero confirmed questions could never be taught, only practiced-and-blocked. Practice itself is untouched and still correctly gates at 3E+2M inside `run_practice_questions`/`build_practice_debrief` ("Practice Not Completed... return once the question bank is ready").
- Decision: Replaced `run_teach_session`'s AI-paraphrased lesson explanation (`lesson_bank.get_validated_lesson()` / `coach.explain_topic()`) with the raw official doc text, shown verbatim.
  - Reason: User's explicit call after seeing the AI-authored "1. Core Concept" panel in the live CLI: for exam prep, the learner should read the actual official MongoDB documentation, not an LLM's restatement of it -- consistent with this project's existing philosophy that LLM output is never trusted as ground truth for facts (citation verification is deterministic-only, self-consistency check never judges MongoDB correctness). The 6-section lesson template (Level Breakdown, Syntax Do/Don't, Exam Radar, Micro-Challenge, 30-Second Recall) is no longer shown in this flow as a result -- a known, accepted tradeoff, not an oversight. Follow-up Q&A (`coach.handle_followup`) is untouched.
- Decision: Show one resolved doc at a time (its own panel + a "next doc" prompt) in `run_teach_session`, instead of concatenating every resolved doc into a single panel.
  - Reason: User wants to read every official doc for a concept for thorough understanding, but one at a time, not as one wall of merged text.
- Decision: Added `planner.resolve_concept_docs()` (relative-threshold doc-relevance filter: a candidate must score at least half the top score for the concept) and switched all five duplicated `prioritize_md_files` + `score_md_file_for_concept(...) > 0` call sites (`load_md_context`, `lesson_bank.py`, `nightly_seed_questions.py`, `map_questions_to_docs.py`, `inspect_doc.py`) to use it.
  - Reason: Live investigation of the merged BSON Data Types panel found the flat `score > 0` threshold let a single generic token ("data", from "BSON Data Types") falsely match two unrelated topic-level docs (`core_data_modeling_introduction`, `core_databases_and_collections`) purely because "data" is a substring of their filenames -- confirmed via `score_md_file_for_concept`: bson_types.md scored 25 (matches "bson" + "types"), the two false positives scored 10 each (match "data" only). A relative threshold (score >= 50% of the concept's top score) drops both false positives while verified-safe against a real multi-doc concept (`sort/limit/skip`'s three cursor docs, which score 10/10/10 -- all tied, all kept). This bug wasn't cosmetic -- `nightly_seed_questions.py` cites through the same `load_md_context` path, so live generation was almost certainly pulling citation context from irrelevant docs for any concept whose name contains a common word.
- Decision: Added `question_targets.weighted_target_for_concept()` + `default_total_bank_target()` (the latter moved out of `nightly_seed_questions._default_total_bank_target`, now a shared public function) and wired a "Coverage vs. exam-weighted target" table into `certcoach-preview-concept`'s dry-run yield report.
  - Reason: User wants the dry-run to show how many questions a doc can sustain "with respect to the weightages," not just a raw candidate-fact count with nothing to measure it against. The new table compares a doc's verified yield to the concept's real weighted Easy/Medium target and reports a plain-English shortfall (e.g. BSON Data Types: this doc alone covers 3/16 of the weighted target).
- Decision: Raised `POPULATION_SOURCE_CHARS` default from 1600 to 8000 (`.env.example` updated to match).
  - Reason: Corpus-wide check found the 1600-char cap (applied identically in both the real generation prompt, `nightly_seed_questions.py:987`, and the dry-run) left even the median doc (~5,728 chars) only ~28% visible, and the long tail (p90 37,594 chars, max 121,920 chars) under 5% visible -- confirmed live for BSON Data Types (23,941 chars): only the intro + type table were ever shown to the generation model, never ObjectId structure, Decimal128, Timestamps, Date construction, or Binary subtypes. 8000 chars leaves headroom inside the primary local model's 4096-token context budget (`get_population_num_ctx`) even with a full `avoid_block` (up to 12 existing questions) included. Live-verified: candidate facts found for BSON Data Types rose from 5 to 8 after the change.
  - Note: verified (citable) count stayed flat at 3 even with more candidates found -- the added candidates mostly failed the verbatim-quote citation check (5 dropped vs. 2 before), meaning quote-paraphrase compliance, not context visibility, is now the dominant yield ceiling. User explicitly deferred fixing this (retry-with-quote-correction or a tightened extraction prompt) for a later session.
- Decision: Rebuilt the dry-run to scan a doc section-by-section instead of one flat truncated blob -- `inspect_doc.chunk_doc_text()` (`langchain_text_splitters.MarkdownHeaderTextSplitter`, already a transitive dependency, no new package) splits on `#`-`####` headers, tags each chunk with its header path + source doc, drops sub-40-char stub chunks, falls back to `RecursiveCharacterTextSplitter` for any oversized section. `inspect_concept()` now scans every chunk individually (own fact-extraction call, own citation check against just that chunk's source file).
  - Reason: User's own brainstormed idea, proposed after seeing the raised-cap fix only partially help. Live result for BSON Data Types (15 sections): verified candidates went from 3 (flat 8000-char truncation) to 66 (chunked scan) -- 73 found, 66 verified, a 90% pass rate vs. 37.5% flat. Chunking incidentally fixed most of the quote-paraphrase problem too (clean header-bounded sections give the model a coherent passage to quote, not an awkward mid-table cutoff), making the previously-deferred quote-verbatim-retry fix far less urgent. Confirms BSON Data Types was never corpus-limited -- flat truncation just never showed the model 14 of 15 sections.
- Decision: Built a new standalone job, `certcoach-generate-from-doc` (`src/certcoach/jobs/generate_from_doc.py`), that generates real quality-gated questions from one concept's chunked+verified facts -- one question per doc section (coverage), style-type distributed by the same exam taxonomy weights `nightly_seed_questions.py` already uses (Type A Syntax/Trap, Type B Theory/Constraints, Type C Predicting Output, Type D Troubleshooting/Performance) -- rather than driven by the exam-weighted quantity target.
  - Reason: User's explicit redirect after the chunking proof-of-concept: "leave weightage aware question generation... focus on getting quality questions based on exam type taxonomy... for the single doc learner just read." Selection = combination of "best fact per section" (doc coverage) and "fixed count, taxonomy-weighted" (per user's own answer), capped at `--max-questions` (default 8), not the 16-slot weighted target. Reuses `nightly_seed_questions.py`'s generation/persistence primitives directly (`StyleTarget`, `generate_weighted_question`, `validate_question_quality`, `is_duplicate_question`, `run_generation_pipeline_checks`, the repair-explanations handoff) rather than reimplementing them, since that logic is provenance-critical (citation verification, self-consistency, duplicate detection) -- only the target-selection or orchestration was rewritten, not the underlying generation/writing guarantees. Added a `generation_source` param to `generate_weighted_question` (default unchanged, `"nightly_weighted_seed"`) so the new pipeline's questions are correctly labeled `"doc_chunk_seed"` in metadata rather than mislabeled.
  - Note: user explicitly stated intent to remove `nightly_seed_questions.py` entirely once this standalone command proves out, at which point its shared generation/persistence primitives should move to a common module rather than staying import-coupled to a file slated for deletion -- flagged here so a future session doesn't treat that import coupling as a permanent design choice.
- Decision: Manually restored `certcoach`'s editable-install import path (a `.pth` file in `.venv/Lib/site-packages` pointing at `src/`) after `pip install -e .` uninstalled the previous registration then failed to complete (the user's own `certcoach.exe` CLI session, PID still running, had the console-script file locked), leaving `import certcoach` broken entirely for a few minutes.
  - Reason: Needed the package importable again immediately to keep testing; a full `pip install -e .` (which also regenerates all console-script `.exe` wrappers, including the new `certcoach-generate-from-doc`) still needs to be re-run once the user's running `certcoach` session is closed -- the manual `.pth` fix restores module imports/pytest only, not the bare console commands.

## 2026-07-07T00:00:00+05:30 (session 8)

- Decision: Locked [[coach_flow_spec|Coach Flow Spec]] as v2.0, rewritten from v1.0's abstract Teach/Check/Practice/Review mode boundaries into a concrete, code-verified stage-by-stage learner journey (Stage 0 daily-agenda selection through Stage 6 Free Chat), with an explicit gate-number reference table and an "out of scope" section for the deferred adaptive layer.
  - Reason: User asked to "lock the flow or journey of the learner" before resuming feature work, given session 7 found the *written* spec had silently drifted from the *real* implementation twice (lesson eligibility wrongly coupled to question-bank readiness; the documented six-section AI lesson already replaced by verbatim doc display). Scoped explicitly to the learner-facing journey (daily agenda -> lesson -> practice -> mock), not the separate content-build/maintainer pipeline (dry-run -> generate -> Docket review -> confirm) that sessions 5-7's uncommitted work mostly touched -- user chose the learner-facing flow first, and chose "write the definitive spec" over live-verifying the CLI again, since the underlying behavior was already freshly confirmed correct in session 7.
  - Note: this was a documentation-only session -- no source files changed, no tests run. The stacked uncommitted work from sessions 5-7 (Docket backend, `generate_from_doc.py`, `review-web/`, CLI cleanup, lesson/doc-source fixes) is still exactly as it was; still ask before committing.

## 2026-07-07T00:00:00+05:30 (session 9)

- Decision: Closed the taxonomy-blind inspection gap. `inspect_doc.CANDIDATE_FACTS_PROMPT` now asks the model for a `suggested_style_type` (Type A-D) per candidate fact, validated in `inspect_concept()` against `nightly_seed_questions.style_weights_for_topic(topic_id)`'s allowed set for that topic (out-of-scope suggestions are cleared to `None`, never guessed). A new `print_taxonomy_yield_report()` shows verified-fact counts per allowed Type plus an "unclassified" row.
  - Reason: User's own walk-through of the content lifecycle ("inspection to see how many questions we can make according to exam style question taxonomy") surfaced that `inspect_doc.py` had zero taxonomy concept at all -- confirmed by grep, zero mentions of "Type A" anywhere in the file -- even though the same taxonomy already existed, fully prompted, in `nightly_seed_questions.py`. Reused the exact same canonical Type A-D names/wording rather than inventing new terminology.
- Decision: Made `generate_from_doc.py`'s style-type assignment content-aware. `select_generation_targets()` now carries each chunk's `suggested_style_type` through; `assign_style_types()` uses it directly when present and valid for the topic, falling back to the existing weighted-random draw only when missing/invalid. Tagged each target with `style_source` ("content" vs "fallback") and surfaced counts of each in `print_summary()`.
  - Reason: Generation was previously 100% blind weighted-random (`random.choices`), completely ignoring the taxonomy signal inspection could now provide -- the user's step 3 ("based on that inspection result we will use the chunks to make the questions") wasn't literally true until this fix. Promoted the duplicated `style_weights_for_topic` (previously defined separately in both `nightly_seed_questions.py`'s inline fallback and `generate_from_doc.py`) into one canonical function in `nightly_seed_questions.py`, imported by both callers -- also collapsed a second inline duplicate of the same weight dict inside `audit_weighted_deficits`.
- Decision: Added a narrowly-scoped `--write` flag to `certcoach-map-questions-to-docs` (`backfill_missing_topics`) that persists `metadata.topic_id`/`metadata.concept` only onto documents currently missing them, using the exact same `resolve_topic_and_concept()` heuristic the read-only report already used -- never overwrites an already-tagged document, never touches `provenance.state`, never fabricates a placement resolution reports as unresolved. Defaults to a dry-run count (no writes) when `--write` is omitted.
  - Reason: The 23 orphan (`metadata.topic_id: None`) legacy questions were invisible to any topic/concept-scoped review query -- `map_questions_to_docs.py` could resolve a placement for most of them but only ever wrote it to an in-memory report/CSV, never back to Mongo. User explicitly approved this as a real (but scope-limited) DB write before implementation.
- Decision: Added a read-only "legacy reference" panel to the review flow -- `database.get_legacy_reference_questions(topic_id, concept, limit)` (returns `[]` unless both scope args are given, never an unscoped dump), `GET /api/legacy` in `review_api.py` (reuses `_serialize_question`), and a `LegacyPanel.jsx` in `review-web/` showing old-bank `suspect` questions mapped to the same concept as the fresh review queue, clearly labeled "not actionable."
  - Reason: User's step 4 ("review both the generated questions and the mapped existing questions") had no home anywhere in the UI -- Docket's queue only ever showed `draft`/`sourced`, and the separate mapping report was never consulted during review. Explicitly chosen as read-only display rather than a second actionable confirm/suspect queue, matching how `suspect` is documented elsewhere in this project as inert/reference-only, never resurrected into an active decision once a question lands there.
- Decision: Removed a batch of confirmed zero-caller dead code: `database.get_all_topics()`/`get_questions_count()`; `planner.get_today_calendar_item()` plus its unused `import random` and unused module-level `RAW_MD_DIR` constant; `dedupe_questions.normalize_question_text()` (and the `re` import it alone used); `nightly_seed_questions.clear_ollama_memory()`/`preload_ollama_model()`/`unload_ollama_model()`/`_question_count()`; `repair_explanations._find_question()` (and the `ObjectId` import it alone used); `config.DEFAULT_REPAIR_MODEL`; `content_contract.LEGACY_CONTRACT_STATUSES`; `question_targets.STUDY_READY_TARGETS`. Deleted `src/certcoach/jobs/lesson_aligned_practice_builder.py` outright (a whole, sizeable Phase-4-era file, zero callers, no entry point, superseded by the current generation pipeline).
  - Reason: User asked to "remove all the dead code and other irrelevant code" alongside the gap fixes. Every removal was confirmed via repo-wide grep (zero callers outside its own definition, `src/scripts/` legacy copy excluded as already-reviewed out of scope) before deletion; a full-package import smoke test plus the full unit suite (311/311) were re-run after the removal to catch anything grep alone might miss. Deliberately left `nightly_seed_questions.py`'s own quantity-target orchestration (`run_weighted_seed`/`main`) untouched -- still the live `certcoach-seed-nightly` entry point, and its retirement was only ever floated as a future decision pending `generate_from_doc.py` "proving out," not yet confirmed.
- Decision: Bumped [[coach_flow_spec|Coach Flow Spec]] to v2.1, adding a "Content Lifecycle" section documenting the now-closed loop (study -> chunk+taxonomy-aware inspect -> content-aware generate -> map/backfill existing -> review fresh + legacy reference -> confirm -> repeat) without restating all of v2.0's learner-facing stages.
  - Reason: Keeps the locked spec's promise (session 8: "the written spec had silently drifted from the real implementation") honest going forward -- the two gaps it previously left undocumented as open questions are now closed in both code and spec in the same session.

## 2026-07-07T00:00:00+05:30 (session 10)

- Decision: Ran a deep audit of `src/certcoach/cli.py`'s menus/options/interactions at the user's request, saved as [[cli_audit_2026-07-07|CLI Behavior Audit]], then fixed all 19 findings plus one additional issue found during the fix pass.
  - Reason: User explicitly asked to "save this for reference and fix all the bugs." The audit itself was three parallel read-only passes (navigation/chrome, study/practice/mock loops, reports/imports) rather than one pass, to cover the whole 3445-line file without missing sections.
- Decision: Centralized `main_menu()`'s exit handling into one `try/except (SystemExit, KeyboardInterrupt, EOFError)/except Exception/finally: exit_message()` wrapping its entire body, replacing ad hoc per-branch handling (only the Settings branch happened to catch `SystemExit` before; the daily-agenda branch and `run_onboarding()` did not).
  - Reason: Found while planning the Ctrl+C fix that the installed `certcoach` console-script entry point calls `main_menu()` directly -- the `if __name__ == "__main__":` graceful-exit wrapper at the bottom of the file never runs for real users, only for `python cli.py`. Combined with the daily-agenda block and onboarding not being wrapped, typing "q" while studying today's agenda (the single most common path) skipped the "Session saved" message and let `SystemExit` propagate raw out of the process. `exit_message()` now fires exactly once, however the function ends.
- Decision: Added a `confirm()` wrapper (mirroring the existing `ask()`) that treats Ctrl+C/EOF as declining -- the safe, non-destructive answer -- and replaced all 8 raw `Confirm.ask(...)` call sites with it.
  - Reason: None of the ~8 `Confirm.ask` sites had any Ctrl+C guard, so interrupting any of them (quit-exam confirmation, mock resume, VRAM clear, etc.) crashed with a raw traceback. Declining is always the safe default for a yes/no confirmation, so one shared, universal Ctrl+C behavior was possible here (unlike `Prompt.ask`, where the safe behavior varies by context).
- Decision: Made mock-exam resume (`run_exam_simulator`) discard the entire saved session -- not just partially restore it -- when the saved question count no longer matches the freshly-drawn set.
  - Reason: The prior code unconditionally restored `user_answers`/`flagged` but only conditionally restored `questions`, so a stale saved session (mock question sets are redrawn randomly each run) could pass the resume check with mismatched array lengths -- `IndexError` crash, or answers/flags silently mapped to the wrong questions. All-or-nothing restore removes both failure modes.
- Decision: Added a "press Enter to continue" pause inside `_render_shortfall_report()`, but only when there's actually a shortfall to show.
  - Reason: Both mock callers printed the shortfall report then immediately called into code that `clear()`s the screen before a human could read it -- the documented "never silent padding, explicit report" guarantee was functionally silent. Gating the pause on "was there anything to report" keeps the common no-shortfall path just as fast as before.
- Decision: Split `run_practice_questions`'s shortfall gate by `is_mock`, and fixed the non-mock branch to check the real fixed composition (`len(unique_easy) < 3 or len(unique_medium) < 2`) instead of the caller's `num`.
  - Reason: The old single gate (`len(questions) < num`) fired the "3 Easy + 2 Medium blocked" message for generic-pool callers (Diagnostic Test, Mini-Mock, Pop Quiz, Boss Fight) that never use that composition at all, and hard-aborted them instead of gracefully running with fewer questions like Full/Timed Mock already do. Separately, the Error Book's `num=3` call could pass the old gate with an invalid mix (e.g. 3 Easy, 0 Medium) since `num` was never actually enforced by the fixed `[:3]+[:2]` slicing -- the gate needed to check the real composition, not whatever `num` a caller happened to pass. Boss Fight was also fixed to print a distinct message when `score is None` (no questions available) instead of reusing the "Boss Defeated You!" loss message.
  - Note: two pre-existing unit tests (`test_run_practice_questions_allows_option_b_answer`, `test_run_practice_questions_next_prompt_q_exits`) were unknowingly relying on the old, looser gate (their fixtures only supplied 1-2 questions total, which the true 3E+2M gate now correctly rejects) -- updated both to supply a real 3E+2M-composition fixture (padding with trivial always-correct filler questions that either aren't reached, in the quit-early test, or are answered alongside the real one, in the other) rather than weakening the gate back to accommodate the tests.
- Decision: Fixed `run_onboarding()` to only persist `exam_date`/`study_calendar` after the final "does this look right?" confirmation, wrapping the whole plan-building flow in an outer loop that retries on "no" instead of just printing broken advice.
  - Reason: The prior code saved both fields *before* asking for confirmation, so answering "no" left the profile populated -- and since `run_onboarding()`'s own skip-gate checks for those exact fields, restarting the app then skipped onboarding entirely into the plan the user just rejected. Also fixed the same-day exam-date boundary bug (comparing `exam_date` at 23:59:59 against `today` at 00:00:00 let today's date incorrectly pass the "must be in the future" check -- now both are compared at end-of-day) and the silent "back" fallback at the experience-level prompt (now re-prompts with a message instead of silently defaulting to Beginner).
- Decision: Fixed `ask()` itself to add back-commands (not just "q") to Rich's `choices` list, and converted `run_account_submenu`/`run_model_manager`'s raw `Prompt.ask(choices=...)` calls to use `ask()`.
  - Reason: Root cause of the "typing back gets rejected" bug in every menu using `ask(..., choices=[...])` -- Rich validates against `choices` before `ask()`'s own back-command check ever runs, so only `"q"` (which was already added) worked; `"back"/"b"/"menu"` were silently rejected and re-prompted forever. Also removed the dead, unreachable `"b"` back-alias in `run_settings_submenu` (shadowed earlier in the same `elif` chain by the Recalibrate option) and made the Scenario Simulator's query prompt honor `BACK_COMMANDS` instead of submitting "back" to the LLM as a literal scenario answer.
- Decision: Made `run_summary_grid_menu` timer-aware (accepts optional `start_time`/`time_limit`, checks expiry each loop iteration and shows a live countdown) instead of only detecting a timeout after the user manually returns to the main exam loop.
  - Reason: The wall clock kept advancing while a learner browsed the grid with no visibility and no enforcement until they left it, producing an abrupt, unexplained auto-submit. Also added a shared `MOCK_SECONDS_PER_QUESTION = 90` constant (previously a bare literal in one place and a stale, inconsistent hardcoded "~28m" pacing message in another) so the two can't drift apart again, retitled "Model Manager" to "Model & Memory Status" since it only inspects/reloads and never actually switches the configured model, and replaced four hardcoded "70%" mastery-threshold strings with the real `status['unlock_threshold_percent']` / `planner.MOCK_EXAM_UNLOCK_THRESHOLD` value (left the unrelated "Weak Areas (Acc < 70%)" accuracy-cutoff string alone -- confirmed during the audit that it's a different concept entirely).
- Decision: Ran the full unit suite after each fix group rather than once at the end, and did not attempt a live manual CLI smoke test for this pass.
  - Reason: All 19 fixes were pure logic/text changes with existing mocked test coverage (311/311 passing); a live smoke test wasn't judged necessary to verify them and wasn't requested.

## 2026-07-08T00:00:00+05:30 (session 11)

- Decision: Ran a full root-level senior-review audit (AI Engineer / QA / CLI-UX / MongoDB / Architect angles) at the user's request, delivered as an artifact rather than in-chat text given its length and tabular content.
  - Reason: Same rationale as the session-10 CLI audit -- a structured, file-specific report is more useful as a standing reference than a wall of chat text, and the user explicitly asked for a specific 10-section format (Summary/Keep/Improve/Remove/AI Coaching/MongoDB/CLI-UX/Tests/Questions/Roadmap).
- Decision: Grounded `handle_followup` (`core/persona.py`) in the same `md_context` `explain_topic` already resolves for the current topic/subtopic, and copied `explain_topic`'s exact "never invent, say so if not covered" guardrail language into `build_followup_prompt`. Left `handle_free_chat` and `evaluate_scenario`/`generate_scenario` unchanged for now.
  - Reason: The audit found `handle_followup` (the mode students use for "wait, does that actually work?") got zero doc grounding and a materially weaker guardrail than the lesson panel sitting right next to it in the same session -- a real, closeable gap since the doc context was already loaded and just never passed through. Free chat was deliberately left looser (genuinely broader-scope, topic-less) rather than forced into the same mold; `evaluate_scenario`/`generate_scenario` were flagged as the single highest-risk gap in the audit but the user didn't ask to fix those this session -- noted as still open.
- Decision: Brought `certcoach-review-questions` (`jobs/review_questions.py`) to feature parity with the Docket web review tool (`review-web/`) instead of committing Docket as-is -- added a `--concept` filter, switched the citation display to `database.get_citation_excerpt`'s in-context highlighted view (previously just printed the bare quote), and added a read-only legacy-reference panel (`database.get_legacy_reference_questions`) matching Docket's.
  - Reason: User's framing was "if we can have the same in CLI, let's have it, or stick to web" -- all three of Docket's actual advantages over the CLI tool turned out to be backend logic already built in `database.py` for the web tool this pipeline never got wired into the CLI, so parity was cheap rather than a rebuild. This reframes the still-open "is `review-web/` committed or disposable" question: once the CLI is at parity, the plan is to retire Docket rather than protect it with a commit, but that retirement hasn't happened yet -- `review-web/` is still uncommitted as of this session.
- Decision: Removed `src/antigravity_cli/` (the `ag` CLI + its Docker/langchain/chromadb-based ingestion engine), `src/scripts/` (the orphaned legacy `core/`+`utils/` tree it drove), `workflows/` (`certcoach_pipeline.yaml` + the generic `example.yaml` placeholder), `.clinerules` (Cline/Antigravity-assistant rules referencing the already-deprecated `memory/active_context.md`), the `ag` entry point and now-fully-unused `pyyaml` dependency from `pyproject.toml`, and the 3 test files that only ever exercised `src/scripts` (`test_indexer.py`, `test_map_mongodb_docs_to_syllabus.py`, `test_mongodb_docs_md_scraper.py`).
  - Reason: User confirmed removal after the audit flagged this as a whole second, unmaintained ingestion pipeline the maintainers themselves had already flagged as "previously undecided" rather than a simple dead-function cleanup. Done in two authorized steps per the auto-mode safety guard: `src/antigravity_cli`/`src/scripts` first (explicitly named), then `workflows`/`.clinerules`/the 3 dependent tests only after a second explicit confirmation, since those weren't named in the first request even though they were direct, predictable casualties of the first deletion. 304/304 tests pass after both steps; `import certcoach.cli` verified clean.
- Decision: Did not touch `update_user_profile`/`update_streak`'s fetch-merge-`replace_one` race this session, despite flagging it as a top-5 fix in the audit.
  - Reason: User clarified CertCoach is single-machine only (corrected an earlier "yes" that had been misread as confirming multi-device use) -- this downgrades that finding from "a real bug you'll eventually hit" back to "worth fixing eventually, not urgent," so it wasn't prioritized this session.
- Decision: Grounded `generate_scenario`/`evaluate_scenario` (`core/persona.py`) in a concretely resolved subtopic doc, closing the audit's single highest hallucination-risk finding, and refactored both from inline-prompt methods into free `build_scenario_prompt`/`build_scenario_evaluation_prompt` functions (mirroring `build_followup_prompt`'s existing pattern) so the prompt content is unit-testable in isolation from a real model call.
  - Reason: User explicitly asked to "deal with the hallucination risk finding" after asking what these two functions were. Previously neither had any doc grounding or MongoDB-correctness guardrail at all -- the model invented the scenario and judged the student's real free-text answer purely from its own unaided knowledge. `run_scenario_simulator` (`cli.py`) picked only a whole topic name with no doc resolution; fixed by having it pick a random subtopic within that topic (mirroring `run_teach_session`'s per-subtopic resolution), resolving that subtopic's official doc via the existing `planner.resolve_concept_docs`/`load_md_context`, and passing the result through to both functions as `md_context`. Both now carry the same "ground in reference material, do not invent, say so if unsure" language already used in `explain_topic`/`handle_followup`. Added 4 new prompt-content tests (`test_persona.py`); `run_scenario_simulator` itself has no existing test coverage to update. 308/308 tests pass.
- Decision: Retired `review-web/` (Docket) entirely rather than committing it -- deleted `review-web/`, `src/certcoach/web/review_api.py`, `tests/unit/test_review_api.py`; dropped the `certcoach-review-web` entry point and the now-unused `fastapi`/`uvicorn` dependencies from `pyproject.toml`; rewrote `requirements.txt` (also dropping the now-dead `pyyaml` line left over from the `antigravity_cli` removal, and re-saving it as plain UTF-8 -- it had drifted to a UTF-16 encoding from an old accidental `pip freeze`); fixed a stale docstring in `cli.py` (`run_question_bank_reports`) that still named `certcoach-review-web` as a sibling tool, pointing it at `certcoach-review-questions` instead.
  - Reason: User said "remove review-web" -- following through on the plan floated across sessions 10-11 once `certcoach-review-questions` (CLI) reached feature parity with it. None of `review-web/`/`src/certcoach/web/` were ever git-tracked, so nothing was lost by deleting outright. 296/296 tests pass (12 fewer than the prior 308 -- `test_review_api.py`'s removal accounts for the difference).
- Decision: Fixed the interrupted `pip install -e .` from session 7 (properly this time, not just the manual `.pth` workaround) and cleaned up a stale corrupted package-metadata directory it left behind.
  - Reason: The original interruption (session 7, the user's own running `certcoach` process had the console-script `.exe` locked) left two artifacts in `.venv/Lib/site-packages`: a manual `certcoach-editable.pth` workaround, and a corrupted `~ertcoach-0.1.0.dist-info` directory (leading `~`, from an interrupted uninstall) that `pip` was surfacing on every subsequent install as a spurious "Ignoring invalid distribution -ertcoach" warning. Re-running `pip install -e .` now that nothing holds the file open completed cleanly and created the proper `__editable__.certcoach-0.1.0.pth`; removed both stale artifacts afterward and re-ran the install once more to confirm the warning is gone. All `certcoach-*.exe` console scripts now exist in `.venv/Scripts/`, including `certcoach-generate-from-doc` as a bare command for the first time.
- Decision: Stopped mid-session on the user's explicit "stop and update all the docs" instruction, before running the remaining live-operation items (Topic 10/11 investigation, `certcoach-map-questions-to-docs --write`, resuming `generate_from_doc.py`, an end-to-end practice-session check) or committing anything.
  - Reason: User asked to pause and leave a clean handoff rather than push through the rest of the list in one sitting. Everything above (review-web retirement, pip install fix) was already complete and verified (296/296 passing) at the time of the stop; nothing was left half-finished. The remaining items are carried forward as-is in [[session_handoff|Session Handoff]]'s Next Action list, not silently dropped.
- Decision: The user decided Claude (this chat) will author MCQs and flashcards directly going forward instead of relying on local Ollama generation, to speed up exam prep; the local model stays exactly where it already is for the self-consistency check and, later, the adaptive coach that will work off real attempt history. Nothing about the existing citation-verify/self-consistency/confirm pipeline changes -- Claude-authored drafts flow through the identical gates local-model drafts already did.
  - Reason: User's own words: "if you help with questions then it will become faster and my preparation will also become faster," with "the adaptive coach can work on my right and wrong attempts with the help of ollama local model" as the explicit boundary. This doesn't weaken the trust model -- the deterministic citation match and human confirm step never trusted the generator's identity in the first place, and a stronger model drafting the content should raise first-pass quality (fewer paraphrase-drift failures) rather than lower it.
- Decision: Audited `data/flashcards.json` (388 lines, 43 cards, byte-identical across `data/`, `mobile/assets/`, `web-flashcards/src/`) before touching anything, per the user's request to check existing content quality first.
  - Reason: Found 27 of 43 cards (63%) had real, concrete defects: unclosed/mismatched Markdown code fences (breaks rendering), literal mid-sentence/mid-code truncation (e.g. card 2.1 cuts off at "// Missing curly braces around th"), and scraped-source artifacts bleeding into the answer text ("Full Practice Set link below", stray "## Section N:" headers) -- clear signs of a bulk import from an external study-guide source, never cleaned up for this app. Also live-DB-checked the 4 currently practice-reachable MCQs (2 confirmed, 2 sourced): 3 are solid (verbatim-cited, substantial explanations, on-topic); 1 -- the Topic 10 "Embedding vs Referencing" `sourced` question -- is a stray, off-topic legacy item ("what stores a list of items?", cited from a screenshot transcript, 0-character explanation) that explains the "Topic 10/11 mystery" flagged since session 7. Recommend marking it suspect rather than letting it get confirmed. A random sample of 8 of the 351 inert `suspect` legacy questions looked like decent raw material, not individually bad -- they're inert by the fresh-start policy decision, not because the content itself is poor.
- Decision: Redesigned flashcards from one long-form note per broad exam objective to atomic, concept-level Q&A cards (one testable fact per card, matching MCQ/syllabus granularity), rather than patching the broken formatting in the old shape.
  - Reason: The old 2-cards-per-topic design bundled many facts into a single multi-paragraph "study note" with embedded code blocks -- reference material, not something recallable in a few seconds, which is the actual point of a flashcard. New schema keeps every field the mobile/web-flashcards frontends already read (`id`, `category`, `domain_weight_pct`, `subheading`, `title`, `question`, `answer`) for zero breakage, and adds `topic_id`/`concept`/`source_doc` (unused by the frontends today, but sets up concept-level remediation matching in `get_remediation_for_wrong_attempt` later instead of only domain-level).
- Decision: Built and shipped a 17-card pilot covering all of Topic 1 (BSON Data Types x7, Document structure x6, Collections vs Tables x4), replacing the old `fc_1_1`/`fc_1_2` cards, grounded directly in the 3 official Topic 1 docs (verified by reading each doc directly, not generated blind). Re-ran the truncation/artifact scan afterward: 0 of the 17 new cards flagged, and the remaining 26 flagged cards are exactly the pre-existing ones outside Topic 1 -- confirms the fix didn't touch anything it shouldn't have. All 3 flashcards.json copies re-verified byte-identical after the edit.
  - Reason: Validate the new format on one topic before committing to rewriting all 58 concepts' worth. Scaling to the remaining 11 topics is queued, pending the user's go-ahead on the pilot.
- Decision: Removed all remaining old-format flashcards (topics 2-6, 41 cards) rather than fixing them in place, leaving only the 17 new Topic 1 atomic cards until replacements are authored via the new workflow.
  - Reason: User's explicit instruction ("let's remove all the old cards then") -- consistent with the redesign-not-patch decision above; the old cards' long-form/domain-level shape is wrong regardless of whether a given card happened to pass the truncation scanner.
- Decision: Built two project-scoped Claude Code skills, `/flashcards` and `/mcqs` (`.claude/skills/{flashcards,mcqs}/SKILL.md`), plus a shared reference doc `memory/content_authoring_guidelines.md` both point at, so future invocations don't need the quality-bar discussion re-derived each time.
  - Reason: User's explicit ask -- "lets have a / command for generating flashcards, MCQs... these should have access to certain files making them truly focused on this task and good quality and have shortest path." Both skills point at: the guidelines doc (quality bar + schema), the syllabus (topic/concept/doc lookup), and the concept's actual official doc -- plus the specific reusable tooling below, so each invocation reuses existing pipeline code rather than re-deriving it.
- Decision: Built `src/certcoach/jobs/flashcard_tools.py` (`certcoach-flashcard-tools` entry point) -- extracts the ad hoc validation (truncation/artifact scan, schema check) and 3-copy atomic merge logic used for the Topic 1 pilot into a reusable, tested module (`validate_cards`, `merge_cards`, both covered by `tests/unit/test_flashcard_tools.py`, 11 tests). `merge_cards` writes all three bundled copies or none, and rejects a merge if a new card's id collides with a surviving old one.
  - Reason: The Topic 1 pilot's validation/merge was done via ad hoc one-off Python during that turn -- not something the new skills could reliably reuse. Extracting it once means every future `/flashcards` invocation validates and merges the same, tested way.
- Decision: Built `src/certcoach/jobs/ingest_authored_content.py` (`certcoach-ingest-authored` entry point) -- takes a directly-authored MCQ (Claude- or human-written, no LLM call) and runs it through the *exact* existing trust pipeline (`is_duplicate_question`, `validate_question_quality`, insert, `run_generation_pipeline_checks` for citation-verify + self-consistency), reusing those functions from `nightly_seed_questions.py` directly rather than reimplementing any of them. Never writes `confirmed` -- always lands at `draft` or `sourced`, same human-confirm step as always.
  - Reason: Direct implementation of the session's earlier design discussion ("shortest path... reuse what exists"): the generation step and the trust-verification step are separable, and only the first one changes when Claude authors content instead of a local model. Caught one real bug while building this: the quality gate requires the seven-part explanation's Syntax Example section to say exactly "not required for this concept" (one of a fixed marker set) for non-syntax-heavy style types (e.g. Type B) -- providing a real code example there anyway *fails* validation, discovered by testing the assembled question against the real (unmocked) `validate_question_quality` rather than trusting the assembly logic. Fixed in `_assemble_explanation` and locked in with a regression test. 12 new tests across `test_ingest_authored_content.py`.
  - Note: also found and fixed a test-isolation bug in the process -- several tests patched `certcoach.jobs.nightly_seed_questions._next_question_number` (the function's *definition* site) instead of `certcoach.jobs.ingest_authored_content._next_question_number` (where `from ... import` actually bound the name used at call time). This meant the tests were silently hitting the real, live, reachable MongoDB instance when run in isolation (passing by accident) and only failed once run as part of the full suite, where some other test's unrelated mock state happened to leak into the same call path. Fixed by patching the importing module, the standard "patch where it's used, not where it's defined" rule; full suite (317/317) and the file in isolation both pass now.


## 2026-07-08T00:00:00+05:30 (sessions 12-14)

- Decision: Authored and merged Topic 2 (CRUD Operations - Create, 15 cards) and Topic 3 (CRUD Operations - Read, 35 cards) flashcards via `/flashcards`, bringing the live set to 67 cards across Topics 1-3.
  - Reason: Continuing the session-11 pivot in canonical syllabus order. Found and worked around (without fixing) a real bug in `planner.resolve_concept_docs()`: its zero-score fallback is `md_files[:2]`, an arbitrary first-N pick rather than a relevance judgment, which silently misfires for any CamelCase-with-`()` concept name (`insertOne()`, `findOne()`, `Projections`, `countDocuments()` all tokenize to fused strings that never match a real filename). Both sessions read every candidate doc by hand and manually assigned sources instead of trusting the resolver's output. Flagged as worth a real code fix once Topic 4 (`replaceOne()`, `updateOne()`, `updateMany()`, `findAndModify`) hits the same class of bug a third time.
- Decision: Ran `/mcqs` for Topic 1 BSON Data Types -- audited all 35 non-confirmed legacy/draft/sourced records, discarded 2 true duplicates of an already-confirmed question, superseded 2 flawed drafts (one failed self-consistency without ever stating its correct answer letter, one was left with an empty explanation from an interrupted session-7 run), and authored 12 new questions to close the gap toward the 16-slot weighted target.
  - Reason: First real Claude-authored MCQ pass since the session-11 pivot, chosen as the starting concept since it already had partial coverage from earlier sessions.
- Decision: After user feedback ("see if we are being too harsh... it's ok to have more genuine questions without duplicates"), went back and authored 4 more distinct questions instead of leaving the 2 discards unreplaced, then did a full categorized review of all 30 pre-existing legacy `suspect` records for the same concept (previously treated as out of scope for a single pass) -- salvaged 3 as fresh questions, corrected 2 factually-wrong ones using the concept's own doc, moved 3 mistagged records to Topic 10 (`Embedding vs Referencing`), and deleted 25 confirmed-zero-value duplicates/filler after backing them up to `backups/bson-data-types-legacy-cleanup-20260708/questions.json`.
  - Reason: Direct user correction of an under-applied rule -- the `/mcqs` skill already said "don't author a replacement unless the concept is short," but the first pass stopped at discard without checking that condition through. Net result: BSON Data Types' suspect count dropped 34 -> 6, and the concept now has 1 confirmed + 18 sourced + 2 draft (comfortably past its weighted target) instead of 1 confirmed + 4 non-suspect records.
- Decision: Codified this feedback directly into `.claude/skills/mcqs/SKILL.md` (Phase A step 4 now has four outcomes -- Keep/Improve/Move/Discard -- plus explicit "don't discard-and-stop when short" and "don't skip the suspect backlog" rules, and a backup-before-delete requirement) rather than leaving it only in personal memory.
  - Reason: Per-conversation memory doesn't travel to a fresh session automatically the way a checked-in skill file does; the user explicitly asked "update all the docs" so future `/mcqs` runs on other topics/concepts inherit this behavior by default instead of needing the same correction repeated.
- Decision: Closed the session by refreshing `agent_context.md`'s Live Snapshot/Immediate Continuation (removing stale session-7 DB numbers and dead `review-web` references) and consolidating `session_handoff.md`'s Next Action list, which had accumulated duplicate/superseded items across sessions 11-14 (e.g. two separate "don't start the adaptive coach" entries, a `generate_from_doc.py` item referencing a decision-log numbering that no longer applied).
  - Reason: User asked to "update all the docs and close the session." `agent_context.md` is still ~1600 words against its documented ~800-word budget (pre-existing, not created this session) -- flagged as a low-priority housekeeping item rather than risking a rushed cut of a still-load-bearing "Non-Negotiable Rule" while closing out.

## 2026-07-09T00:00:00+05:30 (session 15, part 1 -- commits f4eb3c3..47038f6)

- Decision: Built an ephemeral "review quiz" mode (`run_review_quiz`, `cli.py`) that lets the learner self-test against the still-unconfirmed `draft`/`sourced` backlog, separate from the real practice-question path.
  - Reason: The confirmed-question inventory was (and still is) too thin for real practice on most topics; this gives a way to engage with the backlog without it ever touching `save_attempt`/streak/mastery, and without weakening the confirm gate -- confirm/suspect decisions from this mode still route through the identical human-review step `certcoach-review-questions` already used.
- Decision: Swept the full CLI (`cli.py`) for command-handling bugs after a live `/exit` failure, widening `EXIT_COMMANDS`/`BACK_COMMANDS`/`PRACTICE_COMMANDS`/`CONTINUE_COMMANDS` to include slash-prefixed forms and fixing ~10 related issues (case-sensitivity in the exam simulator, `ask()`/`confirm()` wrapper desync, three "type a question to chat" sites that didn't check `BACK_COMMANDS`).
  - Reason: `/exit` was being sent to the AI as a literal chat message instead of being recognized as an exit command -- root cause was `EXIT_COMMANDS` never including slash forms. Fixed globally rather than patching the one site, since the same narrow-set bug recurred at every prompt built before the widened sets existed.
- Decision: Replaced the lesson panel's single flat doc blob with markdown-header-based chunking (`core/doc_chunking.py`, `chunk_doc_text`), showing one section at a time with a "next section / practice" prompt, and fixed the tokenizer bug in `planner.score_md_file_for_concept()` that had been flagged unfixed since sessions 12 and 14 (CamelCase concept names like `insertOne()` now split on word boundaries before tokenizing, so they match real filenames instead of silently falling back to an arbitrary first-2-files pick).
  - Reason: User wanted bite-sized official-doc reading instead of one wall of text, and asked for a corpus-wide count of chunked/split lessons, which is what surfaced the tokenizer bug being exercised at scale. Also found and fixed a real content-loss bug in `topic_06_Query_Operators_L5_01.md` (missing `$nor`/`$not`/`$or` sections vs. the raw source) via a full raw-vs-cleaned heading comparison across all 93 doc pairs.
- Decision: Added ephemeral, non-punitive per-section comprehension check-ins (`CoachPersona.generate_section_check`, `run_section_checkin`), pilot-scoped to one concept (`LESSON_CHECKIN_PILOT_CONCEPTS = {"Document structure"}`), with mixed True/False + occasional MCQ questions, gated on completion but never blocking, and never stored/reused.
  - Reason: User wanted 2-3 comprehension questions per lesson chunk to verify reading, explicitly accepting that this doesn't need the full MCQ trust pipeline (citation-verify/self-consistency/confirm) since it's ephemeral and never shown to a future learner -- same risk category as the already-shipped Scenario Simulator. Found and fixed a real local-model reliability issue during this work: the small study model (`qwen3.5:4b`) reliably stops JSON generation one closing brace short (a formatting habit, not random flakiness) -- `_close_unbalanced_json()` repairs this, raising the live success rate from 0/5 to 5/5 in direct testing.
- Decision: Fixed the invalid Rich color name `"gold"` (should be `"gold1"`) in the EXAM DAY MODE badge and wrapped three unstyled "Press Enter..." prompts in `[dim]`, found via a systematic audit validating every color name in the file against `Color.parse()`.
  - Reason: Final CLI design/UX audit pass before committing the session's work; `"gold"` would have raised at render time the first time that code path was hit live.
- Decision: Committed all of sessions 5-14's stacked, previously-uncommitted work as five logically-grouped commits, then pushed `codex/publish-bank-loop` to `prashanth-ds-ml/MongoDB-Coach-Agent` (not the default `gh` account, which turned out to be a different, wrong-permission GitHub identity -- switched via `gh auth switch --hostname github.com --user prashanth-ds-ml` before retrying).
  - Reason: User's explicit "commit everything as one checkpoint" rather than attempt risky patch-splitting across 10 sessions with no natural commit boundary between them, then explicit "push it" / "push to prashanth-ds-ml" once the wrong-account 403 surfaced.

## 2026-07-12T00:00:00+05:30 (session 15, part 2 -- lesson chunking redesign, mistake-pattern rollup, flashcard tracking)

- Decision: Redesigned `chunk_doc_text` (`core/doc_chunking.py`) from "split at every header, sub-split only if oversized" to "greedily group consecutive small header-sections toward a target size, sub-split only a single section that alone exceeds it" -- opt-in via a new `group_toward_target` param, defaulting to the old behavior so `inspect_doc.py`'s fact-extraction/yield pipeline (tuned against the old behavior) is unaffected. `LESSON_SECTION_MAX_CHARS` raised 1800 -> 2800.
  - Reason: User pushed back on the shipped chunking ("if we have 7 to 9 where 4 to 5 is possible, that's too much") after live use. Root cause: the old algorithm's section count scaled with how many small headers a doc happened to have, not with a target chunk count -- a doc with five ~1-3K-char headers produced 5+ sections even though none were individually oversized. Verified against the real corpus (64 unique resolved docs) that grouping toward 2800 chars gives a median of 3 sections/doc for the typical 5-13K-char doc-length band, while genuinely huge docs (13K-81K chars, ~14 of 64) still scale up proportionally rather than being forced into 4 oversized (~20K-char) chunks. Live-verified on the exact doc the user raised (Document structure): 9-10 sections -> 4.
- Decision: Added `database.get_trap_pattern_report()` (groups the existing error-book `trap_type` classifications by frequency) and wired a "Pattern Summary" panel into `show_error_book_menu()`, and fixed one inconsistency found while auditing CLI styling: the Error Book's "Seven-Part Explanation" panel was the only `Markdown(...)` call in `cli.py` missing `code_theme="monokai"`.
  - Reason: Direct response to the anthropiccertifications.com brainstorm -- "mistake journal with pattern detection" was the one piece genuinely missing that could be built from data already stored (`classify_mistake_trap`/`error_book_col`), no new AI content or trust-pipeline risk. The styling audit chased two dead ends first (a misread closing-tag bug that didn't exist in the actual file, and a heading-hierarchy concern that was an artifact of a test that had disabled terminal styling) before landing on the one real, verified fix.
- Decision: Ran a repo-wide "is CertCoach ready to stop touching and just study" audit. Code side came back clean (376/376 tests, no dead code from the earlier `antigravity_cli`/`scripts` removal, all console_scripts entry points resolve, no secrets). The real finding: only 9 of 355 questions are `provenance.state == confirmed` (is_practice_ready), covering just 2 of 12 topics (Document Model + 1 driver question) -- every High-weight topic (CRUD x4, Query Operators, Arrays, Aggregation, Indexes) has zero practice-ready MCQs. 321 of 330 `suspect` questions share one root cause ("legacy backlog: no verbatim citation quote on record") that needs full re-authoring, not a fix. No automated content-generation job exists anywhere (checked GitHub Actions, this session's scheduler, Windows Task Scheduler) -- the `sourced` queue only grows when generation is run manually.
  - Reason: User wanted to confirm the tool is solid before switching fully into exam prep. This reframed the practice-readiness gap from "maybe a bug" to "a known, honestly-surfaced content-authoring bottleneck" -- the app already degrades gracefully (empty question pools show a clean message, the mission brief doesn't over-promise practice availability) rather than crashing or lying about it.
- Decision: **Superseded the session-7 "defer adaptive coach until 3 topics mastered" gate.** Built flashcard-based spaced review tracking from day one instead: `planner.mark_concept_lesson_seen` (fires unconditionally when a concept's lesson is shown, independent of both the MCQ-score gate and the check-in pilot gate), `planner.compute_next_review`/`record_flashcard_review`/`get_due_flashcards` (SM-2-lite scheduling: correct grows the interval 1->3->x2.3 capped at 60 days, wrong resets to 1 day), `CoachPersona.evaluate_flashcard_recall` (local Ollama model grades a *typed* recall answer semantically, defaulting to `is_correct=False` on any parse failure rather than silently crediting an ungraded answer), and `cli.run_flashcard_review_session()` wired into the Library menu.
  - Reason: The 3-topics-mastered gate assumed the blocker was "not enough usage data," but the audit above showed the real blocker is content-authoring bandwidth for MCQs specifically -- mastery itself depends on the same scarce confirmed-question inventory, so the gate would never actually trigger on its own. User's fix: use flashcards instead of MCQs as the review surface, since flashcards go through a much lighter validation path (`jobs/flashcard_tools.py` -- schema/truncation checks only, no citation-verify/self-consistency/confirm pipeline) and are far cheaper to author, and track from day one rather than wait. Flashcard content coverage is currently just as thin as the MCQ gap (67 cards, topics 1-3 of 12 only) -- the user explicitly chose to build the tracking infra now and let content catch up topic-by-topic. Verified end-to-end against a disposable scratch profile (not real user data) and against the real local Ollama model (3/3 grading test cases correct on the first attempt). 395/395 tests pass after this work.
- Decision: Ran a second, final repo-wide audit after the flashcard work to confirm nothing regressed -- 395/395 tests, all entry points still resolve, `py_compile` clean on every touched file (no linter is configured in this repo; neither `ruff` nor `flake8` is installed), git status shows only the session's own touched files.

## 2026-07-14T00:00:00+05:30 (session 16, commit `9a3b551`)

- Decision: Built a standalone `certcoach-notes` companion command (`src/certcoach/jobs/quick_notes.py`) meant to run in a second terminal alongside the main `certcoach` session, plus a read-only "My Notes" viewer (`show_quick_notes`, wired into the Library menu as option `j`) backed by new `database.add_quick_note`/`get_quick_notes` functions and a new `quick_notes_col` collection.
  - Reason: User wanted a place to jot freeform notes while answering/reviewing questions, for building a personal cheat sheet later -- explicitly not the same thing as the existing per-question `add_question_review_note` (content-improvement signal tied to a `question_id`) or the pre-authored, mastery-gated `show_exam_traps()` sheet. User initially asked for the second terminal to open "automatically"; pushed back on that specific word (fragile on Windows) and proposed a manually-opened standalone command instead, which the user accepted ("yes, build it that way").
- Decision: Added an explicit `0. Back` line to the Review Pending Questions menu (`show_review_queue_menu`, `cli.py`) instead of relying solely on the typed word "back".
  - Reason: User reported "no option to go back" -- not actually a functional bug (`ask()`'s `BACK_COMMANDS` already accepted "back"/"b"), but a real UX gap: the hint was inline text at the end of a long prompt, not a visible numbered option like the rest of the app's menus. `0` was chosen to match the existing 1-N numbered list convention rather than introduce a new pattern.
- Decision: Committed this session's work (`9a3b551`, 6 files) but explicitly left an unrelated batch of modified `cleaned_markdowns/*.md` files (13 files, topics 1/6/7/8/9/10/11) uncommitted and untouched.
  - Reason: Those files were already modified in the working tree at session start, unrelated to the quick-notes work, and their origin/intent is unknown from this session's context (diff shows real content additions, e.g. a previously-missing `$size` example block -- looks like a content re-scrape or repair pass, not damage, but not confirmed). User explicitly chose "commit quick-notes only" when asked, rather than bundle or investigate them now. Not pushed to origin -- per standing instruction, push only when explicitly requested in the same turn.
- Decision: Corrected a stale claim in `session_handoff.md`'s "Current State" section (it said session 15 part 2's work was still "the next commit to land"): confirmed via `git log`/branch tracking that commit `2c2f2b0` already contains that exact work and is already pushed to `origin/codex/publish-bank-loop` (branch shows 0 ahead/0 behind).
  - Reason: Per [[project_certcoach_memory_staleness]] (personal memory) and this file's own repeated "always re-verify, don't trust the last snapshot" pattern -- memory drift is expected between sessions and should be caught at session-close time, not carried forward again.
  - Reason: User explicitly wanted a clean checkpoint before starting exam prep in earnest.
